/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['storage.googleapis.com'], // Adicione os domínios permitidos aqui
    },
  };
  
  export default nextConfig;
  